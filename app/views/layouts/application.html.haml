!!!
%html
  %head
    %title
      Rubyrampage2016- Gitfs Galore
    = csrf_meta_tags

    = stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => 'reload'
    = javascript_include_tag 'application', 'data-turbolinks-track' => 'reload'

  %body
    - if params[:controller] == 'welcome' && params[:action] == 'index'
      = yield
    - else
      #navbar.navbar.navbar-default.yamm{:role => "navigation"}
        .container
          .navbar-header
            %a.navbar-brand.home{"data-animate-hover" => "bounce", :href => "#"}
              = image_tag "/img/logo.jpg", class: 'hidden-xs', alt: 'logo'
              = image_tag "/img/logo.jpg", class: 'visible-xs', alt: 'logo'
          %h2.inline-display
            %i Gifts Galore
          %small Wrap your feeling in beautiful gestures!
      #all
        #content
          .container
            .col-md-12
              = yield